{
  "name": "fitlife-backend-deploy",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}